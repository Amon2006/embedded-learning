今日学习内容：
1、oled屏幕
oled屏幕CH1116的IIC地址为0x7A
通信类型分为两类
（1）指令
指令要以0x00开头，然后是一字节的指令
设置页地址，例：将页地址设置为0则指令为0xB0，将页地址设置为7则指令为0xB7
设置列地址的指令分为两次，例：将列地址设置为90（0x5A），则要发送指令0x0A将列地址低四位设置为A，然后发送指令0x15，将列地址高四位设置为5
设置第0页：0x7A 0x00 0xB0；设置第0列：0x7A 0x00 0x00 & 0x7A 0x00 0x10
（2）数据
数据要以0x40开头，然后发送任意数量的数据
设置完一字节的8个像素后，列地址会自动+1

与oled屏幕通信使用IIC模式，由于数据量较大，需要将IIC的速度从标准模式（Standard Mode）改为快速模式（Fast Mode）改为快速模式（Fast）
将IIC时钟速度从100k提升到400k
由于stm32一直使用内部时钟8M速度运行，为了更快，可以使用外部晶振给stm32提速
System Core————RCC————高速时钟源HSE————晶振Crystal/Ceramic Resonator
随后打开Clock Configuration时钟设置，将HCLK设置为最大值72
Project Manager————Code Generator————为每个外设生成一对.c/.h文件（第一个）
CH1116的第0列的列地址为0x02
memset函数可以将一整块内存都设置为同一个数
memset（数组，要设置的数，数组大小）
