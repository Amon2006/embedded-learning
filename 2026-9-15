今日学习内容：
1、超声波测距模块
控制端Trig、输出端Echo
测量时通过GPIO口向Trig引脚发送一个脉冲信号，超声波模块接收到脉冲信号后就会向外发送一段超声波
紧接着模块将Echo电平拉高，当模块接收到反射回来的超声波后模块便将Echo电平拉低
则Echo高电平的持续时间也即是超声波在往返路途中消耗的时间

向Trig发送脉冲信号即：
HAL_GPIO_WritePin（Trig_GPIO_Port,Trig_Pin,GPIO_Pin_SET）；
HAL_Delay(1);
HAL_GPIO_WritePin（Trig_GPIO_Port,Trig_Pin,GPIO_Pin_RESET）；
将GPIO口先拉高再拉低

输入捕获：
当定时器输入通道上检测到上升沿或下降沿时
立刻将此时计数器的值记录到捕获计时器中，以待程序稍后读取

捕获寄存器
对于通用定时器和高级定时器来说，每个输入通道都有属于自己的捕获寄存器
假设启用输入通道1的输入捕获模式，并且设置为上升沿捕获，定时器启用技术后，若输入到输入通道TI1的信号出现了一个上升沿
则捕获寄存器1便立即将此时计数器的值复制到自身
并且如果为此输入捕获触发了中断，就还会触发输入捕获中断
但是一个输入通道的输入捕获，只能进行上升沿捕获或者下降沿捕获，并不能设置为双边沿捕获
因此TI1输入信号经过边沿检测器后，可分为TI1FP1和TI1FP2
TI1FP1到捕获寄存器1（直接模式），TI1FP2到捕获寄存器2（间接模式）

TIM比较/捕获中断————TIM1 capture compare interrupt
启动输入捕获函数 HAL_TIM_IC_Start（定时器操作句柄指针，启动哪个通道的捕获）
当下降沿来临时，说明高电平结束，因而在通道四捕获到下降沿信号时需要启动中断通知我们
故使用HAL_TIM_IC_Start_IT（定时器操作句柄指针，启动哪个通道的捕获）启用中断
判断产生中断的通道是否为通道四：if（htim->Channel == HAL_TIM_ACTIVE_CHANNEL_4）

读取捕获寄存器数值的函数：HAL_TIM_ReadCapturedValue(定时器操作句柄指针,通道)
