今日学习内容：
1、I2C（IIC）通信
数据线SDA、时钟线（提供同步时钟脉冲）SCL
半双工通信（同一时刻只能进行一个方向的通信）、同步通信
采用主从模式：一台设备为主机，另一台设备为从机。只能由主机先发起通信，从机才能根据主机的指令回复相应的信息
可支持多设备通信————总线协议
在IIC总线上，每个从机都有其唯一的设备地址，如0x70，0x7A
当主机需要操作从机时，只需要在发送数据的最开始发送对应从机的设备地址

串口通信————全双工模式，异步通信
IIC的数据线和时钟线往往都有上拉电阻进行上拉，所以在未开始通信时，数据线和时钟线都处于高电平
IIC通信启动信号：在时钟线依旧是高电平时，将数据线提前下拉
当时钟线处于低电平时，主机设置数据线的电平；当时钟处于高电平时，从机读取数据线的电平
数据的接收方（从机）在数据接收完后需要发送一个ACK信号（应答信号）确认自己已经收到数据，然后发送数据给主机
ACK信号：在时钟线低电平时，从机将数据线拉低一下
从机发送完所有数据后，主机会在时钟线处于高电平时，将数据线拉高，宣告通信的完成

Project Manager————Code Generator————为每个外设生成一对.c/.h文件（第一个）

2、自己定义外设的一对.c/.h文件
Inc————New————Header File
Src————New————Source File

3、IIC通信一般使用7位地址码 0111000————0x38
但IIC通信中每次发送都是一字节，也就是八位
所以规定从机地址左移一位 0111000——>01110000（0x70）/01110001（0x71）
如果主机发起通信是为了设置（写）从机————最后一位为0（01110000）
如果主机发起通信的目的是为了从从机读取数据————最后一位为1（01110001），但是一般默认最后一位为0（HAL相关函数会自动处理）
IIC读取函数 HAL_I2C_Master_Receive(外设指针（&hi2c1）, 要读取的从机地址, 用于接收数据的变量的指针, 读取多少位数据, 超时时间)
IIC发送函数 HAL_I2C_Master_Transmit(外设指针（&hi2c1）, 要读取的从机地址, 用于发送数据的变量的指针, 发送数据的长度, 超时时间)

4、按位与&
两个数据均为1的位保留为1；有一个是0或两个都是0记为0

5、IIC中断
与串口中断类似
打开位置：Connectivty————I2C————NVIC Settings————事件中断、错误中断
中断接收函数HAL_I2C_Master_Receive_IT(外设指针（&hi2c1）, 要读取的从机地址, 用于接收数据的变量的指针, 读取多少位数据)
中断发送函数 HAL_I2C_Master_Transmit_IT(外设指针（&hi2c1）, 要读取的从机地址, 用于发送数据的变量的指针, 发送数据的长度)
static 加在局部变量上 = 值不丢
TxCpltCallback————发送完成回调
RxCpltCallback————接收完成回调

6、IIC的DMA模式
接收函数HAL_I2C_Master_Receive_DMA(外设指针（&hi2c1）, 要读取的从机地址, 用于接收数据的变量的指针, 读取多少位数据)
发送函数HAL_I2C_Master_Transmit_DMA(外设指针（&hi2c1）, 要读取的从机地址, 用于发送数据的变量的指针, 发送数据的长度)